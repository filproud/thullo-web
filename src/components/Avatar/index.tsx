import { Container, Img } from './styles';
import type { Props } from './types';

export function Avatar({ url, ...props }: Props) {
  return (
    <Container>
      <Img></Img>
    </Container>
  );
}
