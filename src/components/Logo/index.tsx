import { Container, Text } from './styles';
import type { Props } from './types';

export function Logo({ ...props }: Props) {
  return (
    <Container>
      <img src="" alt="logo" />
      <span></span>
    </Container>
  );
}
